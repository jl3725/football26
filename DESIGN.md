# Football Analytics RAG — 설계 및 타당성 문서

> 목표: football database를 기반으로 **선수/팀 분석·추천**을 수행하는 시스템을 Graph RAG + Vector RAG로 구축할 수 있는가? (FM26 스타일의 통계적 인사이트 제공)

---

## 1. 타당성 결론

**가능합니다.** 단, 핵심은 "RAG로 시뮬레이션을 흉내내는 것"이 아니라
**RAG를 분석·추천·설명 레이어로 쓰는 것**입니다.

- 선수/팀 **분석·추천**은 RAG가 가장 잘하는 영역입니다.
- "비슷한 선수 찾기", "내 전술에 맞는 영입 추천", "이 선수의 약점 설명"
  → Vector(의미 유사) + Graph(관계 추론) 조합으로 자연스럽게 풀립니다.
- 경기/시즌 시뮬레이션(FM의 매치 엔진)은 별개의 통계 모델 영역이며,
  이번 1차 목표(분석·추천)에는 필수가 아닙니다. (→ 9장 향후 확장)

---

## 2. 데이터 소스 (현재 보유 데이터 없음 → 추천)

| 소스 | 내용 | 라이선스 | 추천도 |
|---|---|---|---|
| **StatsBomb Open Data** | 이벤트 단위(패스/슛/압박) 상세 데이터, xG 포함 | 무료(비상업) | ★★★ 분석용 최고 |
| **FBref / Opta 파생** | 시즌별 선수 집계 스탯(90분당 지표 등) | 스크래핑 주의 | ★★★ 추천용 핵심 |
| **Kaggle: FIFA/FM player datasets** | 선수 능력치(0~99), 포지션, 가치 | 무료 | ★★ FM 감성 |
| **Transfermarkt** | 이적 이력, 시장가치, 계약 | 스크래핑 주의 | ★★ 그래프용 |
| **football-data.org / API-Football** | 경기 결과, 일정, 라인업 | 무료~유료 | ★ 보강용 |

> 추천 1차 조합: **FBref(시즌 스탯) + Kaggle FIFA(능력치) + Transfermarkt(이적 관계)**
> - 스탯 = Vector 임베딩 재료
> - 이적/소속/감독 관계 = Knowledge Graph 재료

---

## 3. 전체 아키텍처

```
                ┌──────────────────────────────┐
                │        Football DB (raw)      │
                │  선수·팀·경기·이적·스탯 테이블   │
                └───────────────┬──────────────┘
                  ETL / 전처리   │
          ┌────────────────────┼────────────────────┐
          ▼                                          ▼
┌────────────────────┐                   ┌────────────────────────┐
│   Vector Store      │                   │   Knowledge Graph       │
│  (선수 스탯/리포트   │                   │   (Neo4j)               │
│   임베딩)           │                   │  (선수)-[:PLAYS_FOR]->  │
│  pgvector / Qdrant  │                   │  -[:TRANSFERRED]->      │
└─────────┬──────────┘                   └────────────┬───────────┘
          │  의미 유사 검색                  관계/경로 탐색 │
          └────────────────┬───────────────────────────┘
                           ▼
                ┌────────────────────────┐
                │   LLM Orchestrator       │
                │  (Claude — 질의 라우팅,   │
                │   근거 종합, 설명 생성)    │
                └────────────────────────┘
                           ▼
                  자연어 분석·추천 결과
```

---

## 4. Vector RAG 설계 (의미적 유사 — "비슷한 선수")

**아이디어:** 선수를 벡터로 표현 → 코사인 유사도로 "닮은 선수" 검색.

두 가지 임베딩 전략:
1. **스탯 벡터 (정량)**
   - 90분당 지표(슈팅, 키패스, 태클, 전진패스, 압박 등)를 정규화한 수치 벡터
   - 장점: 해석 쉬움, 빠름 / "플레이 스타일 유사" 검색에 직접적
2. **텍스트 임베딩 (정성)**
   - 선수 스카우팅 리포트/요약 문장을 LLM 임베딩으로 변환
   - "압박 강한 라인 브레이커형 윙어" 같은 자연어 질의 매칭

> 1차 추천: **정량 스탯 벡터** 먼저. 데이터가 깔끔하고 설명 가능성이 높음.
> 이후 정성 텍스트 임베딩을 하이브리드로 추가.

**예시 질의** → "손흥민과 비슷한데 더 저렴한 윙어 5명"
→ 손흥민 스탯 벡터 최근접 이웃 + 시장가치 필터.

---

## 5. Graph RAG 설계 (관계 추론 — 축구의 본질)

축구 데이터는 **관계 그래프**입니다. Graph RAG가 특히 강한 영역.

**노드:** Player, Team, League, Manager, Position, Season
**엣지 예시:**
- `(Player)-[:PLAYS_FOR {season}]->(Team)`
- `(Player)-[:TRANSFERRED {fee, date}]->(Team)`
- `(Team)-[:MANAGED_BY]->(Manager)`
- `(Player)-[:TEAMMATE_OF {season, minutes_together}]->(Player)`

**Graph RAG로만 풀리는 질문:**
- "A선수와 같이 뛸 때 성적이 오른 미드필더는?" (팀메이트 시너지 경로)
- "이 감독이 선호하는 선수 유형은?" (감독→영입 패턴 집계)
- "유망주 육성 계보 / 이적 네트워크 허브 팀은?"
- "우리 전술(4-3-3 게겐프레싱)에 맞는 영입 후보" (포지션·스타일 그래프 매칭)

**구현:** Neo4j + Cypher. LLM이 자연어 → Cypher 변환(Text2Cypher) 후
결과 서브그래프를 근거로 답변 생성.

---

## 6. 하이브리드 전략 (둘을 합치는 법)

분석·추천 품질은 **Vector + Graph 결합**에서 나옵니다.

```
사용자 질의
   │
   ▼
[LLM 라우터] ── "유사성 질문?" → Vector 우선
   │           "관계 질문?"   → Graph 우선
   │           "복합?"        → 둘 다
   ▼
1) Vector: 스탯상 비슷한 후보군 추출 (recall)
2) Graph: 후보를 관계 조건으로 필터·랭킹
          (예: 우리 리그 적응 사례, 감독 호환, 나이/계약)
3) LLM: 두 근거를 종합해 추천 + 이유 설명
```

> 예: "우리 팀에 맞는 윙어 추천"
> → Vector로 스타일 유사 후보 50명 → Graph로 "비슷한 전술 팀에서 성공" +
>   "예산 내 시장가치" 필터 → 상위 5명 + 근거 문장.

---

## 7. 기술 스택 제안

| 레이어 | 1차 추천 | 대안 |
|---|---|---|
| 원본 DB | PostgreSQL | SQLite(소규모 PoC) |
| Vector | **pgvector**(PG에 통합) | Qdrant, Chroma |
| Graph | **Neo4j** (Community) | Memgraph, NetworkX(소규모) |
| 임베딩/LLM | **Claude API** (`claude-opus-4-8` / `claude-haiku-4-5`) | — |
| 오케스트레이션 | Python (LangGraph 또는 직접 구현) | — |
| 프론트 | 추후 (CLI/노트북부터) | Streamlit |

> pgvector를 쓰면 초기엔 DB 하나로 정량 분석 + 벡터 검색을 모두 처리 가능 →
> 복잡도 ↓. Graph가 진짜 필요해지는 시점에 Neo4j 추가.

---

## 8. 단계별 로드맵

**Phase 0 — 데이터 확보 (1주)**
- FBref 시즌 스탯 + Kaggle FIFA 능력치 1개 리그(예: EPL) 수집·정제

**Phase 1 — Vector 추천 PoC (1~2주)**
- 스탯 정규화 → pgvector 적재 → "비슷한 선수" 검색 동작
- ✅ 검증: "손흥민 유사 선수 Top 10"이 직관과 맞는가

**Phase 2 — Graph 추가 (2주)**
- Transfermarkt 이적/소속 → Neo4j 적재
- Text2Cypher로 관계 질의 응답

**Phase 3 — 하이브리드 + LLM 설명 (2주)**
- 라우터 + Vector/Graph 결합 + 근거 기반 추천문 생성

**Phase 4 (선택) — 시뮬레이션** → 9장

---

## 9. 향후 확장: FM 스타일 시뮬레이션 (분석·추천과 분리)

RAG가 아니라 **통계 모델** 영역:
- 경기 결과: Poisson / Dixon-Coles, 팀 강도: Elo, 득점 기대: xG 기반
- RAG는 여기서 "왜 이런 예측인가"를 **설명**하는 역할로 결합 가능

---

## 10. 리스크 / 한계

| 리스크 | 대응 |
|---|---|
| 데이터 스크래핑 라이선스 | 공개 데이터(StatsBomb) 우선, 출처 명시 |
| LLM이 수치를 지어냄(환각) | 추천은 DB 검색 결과만 근거로, LLM은 "설명"만 |
| Graph 구축 비용 | Phase 1은 Vector만으로 가치 검증 후 Graph 투입 |
| "유사 선수"의 주관성 | 지표 가중치/포지션 보정 튜닝 필요 |

---

## 다음 액션 제안

1. **데이터 1차 확보** — 어느 리그/시즌부터 할지 정하기 (예: EPL 23/24)
2. **Phase 1 PoC** — pgvector 기반 "비슷한 선수 추천" 최소 코드부터
   → 가치가 가장 빨리 보이고, Graph 투입 판단의 근거가 됨
